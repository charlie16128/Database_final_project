# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

.eslintcache

# Cypress
/cypress/videos/
/cypress/screenshots/

# Vitest
__screenshots__/

# Vite
*.timestamp-*-*.mjs

# ── 本專案新增 ──────────────────────────────

# SQLite 執行時暫存檔（由 SQLite WAL 模式自動產生，每台電腦不同，不需 commit）
# server/db/database.sqlite-shm
# server/db/database.sqlite-wal
# server/db/*.bak

# database.sqlite 本體保留追蹤（共同範例資料）
# server/db/database.sqlite  <-- 此行保持註解，代表不忽略

# 使用者上傳的圖片（保留追蹤，每次上傳後需手動 commit）
# server/uploads/  <-- 此行保持註解，代表不忽略

# 環境變數（含 JWT_SECRET 等機密）
.env
.env.*
!.env.example

# Windows 縮圖快取
Thumbs.db
ehthumbs.db
Desktop.ini

